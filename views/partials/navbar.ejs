<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">DestinationNext</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link active text-white-50" href="/">Home</a>
                <a class="nav-link text-white-50" href="/parks">Places</a>
                <a class="nav-link text-white-50" href="/parks/new">New Place</a>
            </div>
            <div class="navbar-nav ms-auto">
                <% if(!currentUser){ %>
                    <a class="nav-link text-white-50" href="/register">Sign up</a>
                    <a class="nav-link text-white-50" href="/login">Login</a>
                    <% } else { %>
                        <a class="nav-link text-white-50" href="/logout">Sign out</a>
                        <% } %>
            </div>
        </div>
    </div>
</nav>